<?jelly escape-by-default='true'?>
<j:jelly xmlns:c="/lib/credentials" xmlns:j="jelly:core" xmlns:f="/lib/form">

    <f:entry field="authenticationType" title="Authentication type">
        <f:select />
    </f:entry>

    <f:entry field="credentials" title="Credentials">
        <c:select/>
    </f:entry>

    <f:entry field="folder" title="Folder">
        <f:textbox />
    </f:entry>

    <f:entry field="jsonFile" title="JSON file path">
        <f:textbox />
    </f:entry>

    <f:entry field="files" title="Multiple files selector">
        <f:textbox />
    </f:entry>

    <f:entry field="path" title="Path">
        <f:textbox />
    </f:entry>

    <f:entry field="addBuildInfo" title="Add build info">
        <f:checkbox />
    </f:entry>

    <f:entry field="awaitProcessing" title="Wait for processing">
        <f:checkbox default="true" />
    </f:entry>

    <f:entry field="processingTimeout" title="Processing timeout (seconds)">
        <f:number default="300" />
    </f:entry>

    <f:entry field="failOnChanges" title="Fail build on detected changes">
        <f:checkbox />
    </f:entry>

    <f:entry field="quiet" title="Quiet mode">
        <f:checkbox />
    </f:entry>

</j:jelly>
